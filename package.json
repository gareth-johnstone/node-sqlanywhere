{
  "author": "SQL ANYWHERE",
  "name": "sqlanywhere",
  "description": "SQL Anywhere JavaScript Driver.",
  "version": "2.0.3",
  "repository": {
    "url": "https://github.com/gareth-johnstone/node-sqlanywhere"
  },
  "main": "./lib/index",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "node-addon-api": "^8.5.0",
    "node-gyp-build": "^4.8.4",
    "prebuild-install": "^7.1.3"
  },
  "scripts": {
    "install": "prebuild-install || node-gyp rebuild",
    "build": "node build.js",
    "test": "node examples/test.js",
    "release": "npm version patch && git push --follow-tags"
  },
  "devDependencies": {
    "dotenv": "^17.2.2",
    "prebuild": "^13.0.1"
  },
  "binary": {
    "napi_versions": [
      6
    ]
  },
  "volta": {
    "node": "22.19.0"
  }
}
